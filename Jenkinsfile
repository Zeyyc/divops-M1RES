pipeline {
  agent any

  stages {
    stage('Checkout Automatique') {
      steps {
        echo ' Le code a été récupéré automatiquement par Jenkins.'
        sh 'pwd'
        sh 'ls -la'
      }
    }
  }
}
